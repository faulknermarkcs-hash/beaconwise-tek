services:
  - type: web
    name: transparency-ecosphere-kernel
    env: python
    buildCommand: pip install -r requirements.txt && pip install .
    startCommand: bash -lc "export PYTHONPATH=$PWD/src:$PYTHONPATH && streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
